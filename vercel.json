{
    "version": 2,
    "builds": [
        {
            "src": "backend_code/app.py",
            "use": "@vercel/python",
            "config": {
                "maxLambdaSize": "15mb"
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "backend_code/app.py"
        },
        {
            "src": "/(.*)",
            "dest": "backend_code/app.py"
        }
    ],
    "env": {
        "PYTHON_VERSION": "3.9"
    }
}